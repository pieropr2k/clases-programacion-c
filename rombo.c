#include <stdio.h>

int main() {
    int n = 7; // Tamaño del rombo, debe ser impar
   	if (n % 2 == 0) {
        printf("El tamaño del rombo debe ser un número impar.\n");
        return;
    }
    // complete
}

